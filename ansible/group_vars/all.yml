---
docker_apt_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common

docker_compose_version: "1.29.2"

app_deploy_path: "/opt/weather-app"

registry_url: "{{ lookup('env','CI_REGISTRY') }}"
image_tag:   "{{ lookup('env','CI_COMMIT_SHA') | default('latest') }}"
